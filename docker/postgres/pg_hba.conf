# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE  USER  ADDRESS            METHOD

# Local socket connections (inside container)
local   all       all                       trust

# IPv4/IPv6 localhost
host    all       all     127.0.0.1/32      scram-sha-256
host    all       all     ::1/128           scram-sha-256

# Default Docker bridge networks (container-to-container)
host    all       all     172.16.0.0/12     scram-sha-256

# External BI / DataLens access (replace with real IPs/CIDR and user)
# host  all  datalens_readonly 203.0.113.10/32  scram-sha-256